# WeatherAndAPITestProject

This is a simple framework to test Rest API and UI of a wesite together using TestNG framework. 

<b> Steps To Execute : </b>

1. git clone (clone link)
2. mvn clean install
3. run through the testng.xml file 
